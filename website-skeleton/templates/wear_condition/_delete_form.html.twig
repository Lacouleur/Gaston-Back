<form method="post" action="{{ path('wear_condition_delete', {'id': wear_condition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wear_condition.id) }}">
    <button class="btn">Delete</button>
</form>
